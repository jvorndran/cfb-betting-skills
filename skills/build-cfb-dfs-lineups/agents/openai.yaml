interface:
  display_name: "Analyze and Build CFB DFS"
  short_description: "Analyze CFB DFS and build on request"
  default_prompt: "Use $build-cfb-dfs-lineups to analyze this CFB DFS slate, and construct validated lineups only if I explicitly request them."
