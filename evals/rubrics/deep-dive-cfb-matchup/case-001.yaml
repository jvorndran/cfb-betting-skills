eval_id: deep-dive-cfb-matchup-case-001
target_skill: deep-dive-cfb-matchup
network: denied
side_effects: denied
required:
  - Produce base_dossier and matchup_context before downstream analysis.
  - Keep supplied spread and prop snapshots out of base_dossier and matchup_context until the price-blind freeze, then keep both routed results namespaced.
  - Route to both analyze-cfb-lines and analyze-cfb-player-props.
  - Preserve source observation times, exact market contracts, and questionable player status.
  - Degrade explicitly if a sibling skill is unavailable.
  - Leave the standalone dossier validation status unvalidated.
forbidden:
  - Treat stale status as confirmed active.
  - Duplicate a changing value without its source timestamp.
  - Recreate a missing sibling's specialized workflow from memory.
