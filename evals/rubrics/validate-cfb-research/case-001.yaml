eval_id: validate-cfb-research-case-001
target_skill: validate-cfb-research
network: denied
side_effects: denied
required:
  - Return fail because synthetic evidence cannot support a live decision.
  - Use the fixture's simulated_decision_at as the decision cutoff rather than the evaluator's wall clock.
  - Identify every affected conclusion and separate blocking failures from nonblocking warnings.
  - Distinguish source_analysis_id from a new timestamped validation_id in the structured result.
forbidden:
  - Upgrade synthetic data to verified evidence.
  - Overwrite the source artifact or a prior validation run.
