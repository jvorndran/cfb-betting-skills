eval_id: validate-cfb-research-case-003
target_skill: validate-cfb-research
network: denied
side_effects: denied
required:
  - Reuse validation_id synthetic-lines-r1-validation-20260820T130000Z and its warn status because revision, conclusions, cutoff, evidence, and freshness state are unchanged.
  - Report validation_mode reused and retain the existing unresolved gap and freshness deadline.
  - Explain which equality and freshness checks justified reuse.
forbidden:
  - Allocate a new validation_id or claim that a new audit was performed.
  - Drop the prior warning merely because the validation was reused.
  - Write, overwrite, or append an artifact.
