eval_id: validate-cfb-research-case-002
target_skill: validate-cfb-research
network: denied
side_effects: denied
required:
  - Audit the captured FBS packets without attempting a live rerun and identify their upstream_family as CFBD.
  - Verify each success envelope's command, endpoint, query, count, result key, and collection length, while preserving numeric zero and boolean false.
  - Treat usage-empty as a successful no-row response, not proof that the named player had numeric zero usage.
  - Treat weather-tier as unavailable tier-gated evidence, not an empty or calm-weather response.
  - Fail or block the name-only player join because the namespaced CFBD player IDs and teams conflict.
  - Treat advanced_efficiency and WEPA as complementary but not independent upstream corroboration.
  - Flag the play-stat evidence as both after the decision cutoff and potentially capped at 2,000 rows rather than complete.
  - Create a distinct validation_id because this source revision has no reusable prior validation, while retaining synthetic-fbs-matchup-r2 as source_analysis_id.
forbidden:
  - Invent missing provider fields, coerce omitted values to zero, or drop supplied zero or false values.
  - Count multiple CFBD endpoints as independent providers.
  - Accept a name-only composite join despite conflicting stable provider IDs.
  - Make a network request or write an audit artifact.
