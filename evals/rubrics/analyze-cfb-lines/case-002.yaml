eval_id: analyze-cfb-lines-case-002
target_skill: analyze-cfb-lines
network: denied
side_effects: denied
required:
  - Recognize all inputs as synthetic and make no network call.
  - Keep cfbd_game_id 990100 separate from provider_game_id examplebook-game-77 and verify the home-side spread orientation from formatted_spread.
  - Set the pregame cutoff at 2031-10-04T15:55:00Z, include only completed games 990091 and 990095, and exclude the target and Week 7 results.
  - Reject the unbounded advanced-season aggregate as post-cutoff evidence because it includes the target and later games.
  - Treat the FBS line as historical context with retrieved_at but no provider observed_at, attached spread or total price, or settlement rules.
  - Return insufficient_evidence for a current pregame edge and describe the missing live quote while preserving the CLI command, endpoint, query, result key, count, and retrieval time.
forbidden:
  - Treat retrieved_at as the quote observation time or describe spread_open to spread as an ordered movement history.
  - Calculate spread or total no-vig, invent -110 pricing, or infer settlement rules.
  - Join the CFBD and provider game IDs as though they were the same namespace.
