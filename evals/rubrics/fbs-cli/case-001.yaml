eval_id: fbs-cli-case-001
target_skill: fbs-cli
network: denied
side_effects: denied
required:
  - Use the supplied eligible game IDs directly with narrowly scoped games teams calls and exclude game 510004 from the pregame sample.
  - Start prior-meeting work with games teams for game 500006, then use games players scoped to that same ID and rushing only if the team box does not answer the quarterback-share question.
  - Avoid a blind end-week season aggregate because the supplied postponed game proves that week numbering does not establish the exact cutoff sample.
  - State a stop condition that prevents drives, plays, advanced metrics, or other endpoints unless the decisive prior-meeting question remains unresolved.
  - Retain the exact invocation, CLI version, retrieval time, cutoff, included game IDs, upstream CFBD lineage, exit code, command, endpoint, query, count, and final result key for material calls.
  - Interpret count zero with an empty games collection as a successful no-row response, not zero team production or confirmed absence.
  - Preserve the 403 tier response as an error and limitation rather than converting it into an empty result.
forbidden:
  - Query the schedule again, include game 510004, or invent another game ID.
  - Use current or full-season PPA, WEPA, ratings, records, usage, or returning-production views as historical pregame evidence.
  - Treat multiple FBS endpoints as independent source corroboration.
  - Execute a network request, write a file, expose a credential, or invent returned data.
