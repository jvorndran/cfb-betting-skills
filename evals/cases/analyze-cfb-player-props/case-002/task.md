# Task

Use `$analyze-cfb-player-props` with [the synthetic player and FBS evidence](synthetic-player-evidence.json) to assess Alex Example's role sample for the supplied Week 6 passing-yards prop. Present the result as a concise report in chat. Network access is denied; use only the supplied data and identify which rows or aggregates are ineligible or incomplete at the decision cutoff.
