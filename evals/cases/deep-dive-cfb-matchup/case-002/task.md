# Task

Use `$deep-dive-cfb-matchup` with [the synthetic matchup fixture](../../../fixtures/synthetic-matchup-deep-dive.json) to produce an individual matchup deep dive using the host's native interactive visualization or artifact feature. Do not perform line, player-prop, or DFS analysis. Network access and filesystem writes are unavailable, so keep the dossier and structured handoff in the response.
