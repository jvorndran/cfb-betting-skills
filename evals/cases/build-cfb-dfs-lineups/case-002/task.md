# Task

Use `$build-cfb-dfs-lineups` with [the synthetic provider slate and FBS enrichment](synthetic-dfs-input.json) to build and validate one candidate lineup. Present the result as a concise report in chat. Network access is denied; use the provider data for roster legality and explain how the CFBD evidence may and may not affect construction.
