# Task

Use `$analyze-cfb-lines` with [the synthetic FBS evidence](synthetic-fbs-evidence.json) to assess whether the CFBD line record establishes a pregame edge for Piedmont State at the stated decision time. Present the result as a concise report in chat. Network access is denied; use only the supplied data and explain any evidence that cannot be used at the cutoff.
